# Copy to .env and fill in. Do not commit .env.

# LLM (choose the vars your provider expects — see CrewAI LLM docs)
OPENAI_API_KEY=
# OPENAI_MODEL_NAME=gpt-4o-mini

# Optional: other providers
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=

# Optional tooling (enable in crew.py / agents when used)
# SERPER_API_KEY=
